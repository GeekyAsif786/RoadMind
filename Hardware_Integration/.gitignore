# Secrets / credentials (WiFi, device tokens, base URLs)
secrets.h
**/secrets.h
*.secrets.h

# Local helper scripts (not for the repo)
rotate_device_credential.sh
update_device_secrets.sh

# Arduino / PlatformIO build artifacts
build/
.build/
*.bin
*.elf
*.hex
*.map
.pio/
.pioenvs/
.piolibdeps/

# Editor / OS cruft
.DS_Store
.vscode/
